# AGENTS.md

## Logic Entry

This repository follows the current `UIhoshi/AgentLogic` main logic line.

Start each work round in this order:
1. Read `README.md`.
2. Read the docs hub entry listed in `README.md`.
3. Read this `AGENTS.md`.
4. Confirm whether the current task extends the previous coding round.
5. If it does not, ask whether context should be forgotten/compressed before continuing.

## Logic Source Of Truth

- The active logic source of truth is the current `UIhoshi/AgentLogic` repository and the installed main logic derived from it.
- Keep repository docs aligned to the current main logic, but do **not** upload copied AgentLogic package markdown into this repo.

## Repository Boundaries

- Allowed to sync: project docs, source code, and intended release assets.
- Do **not** sync workspace-private logic markdown, copied rule bundles, build residue, temporary screenshots, logs, or packaging leftovers.

## Project Focus

- Docs hub: `PROJECT_GUIDE_AND_README/`
- Client code: `client/`
- Server code: `server/`
- This repo is Windows / LAN / offline-first in its operational assumptions.
